# MasterNewLanguage — フランス語学習アプリ

## プロジェクトの目的

最終目標は **フランス語の小説・記事を原文で読めるようになること**。
そのための語彙力・読解力を、**1日10分から続けられる**形で鍛えるアプリを作る。

設計上の前提:

- スマートフォンのブラウザから使えること（レスポンシブな静的 SPA）
- サーバー不要。学習データはブラウザの localStorage に保存する
- 1回の学習セッションが10分以内で区切れること

## 技術スタック

| 領域 | 採用 |
|---|---|
| ビルド | Vite 8 |
| UI | React 19 + TypeScript（strict） |
| テスト | Vitest + Testing Library（jsdom） |
| Lint / Format | Biome（lint と format を兼ねる） |
| データ保存 | localStorage（将来 IndexedDB に差し替え可能） |

## コマンド

すべてリポジトリルートで実行する。初回のみ `npm install`。

| 目的 | コマンド |
|---|---|
| **テスト実行**（1回・CI用） | `npm test` |
| テスト監視モード | `npm run test:watch` |
| **Lint チェック**（変更なし） | `npm run lint` |
| Lint 自動修正 | `npm run lint:fix` |
| フォーマット | `npm run format` |
| **ビルド**（型チェック + 本番ビルド） | `npm run build` |
| 開発サーバー | `npm run dev` |
| **一括検証**（lint → test → build） | `npm run verify` |

受け入れ判定に使うのは `npm run verify`。
これが最後まで成功しない限り、そのタスクは合格ではない。

## ディレクトリ構成

```
src/
  main.tsx                     エントリポイント
  App.tsx                      画面のルート
  test/setup.ts                テスト共通セットアップ
  features/
    vocabulary/                語彙学習機能
      types.ts                 ドメイン型（Word, WordProgress など）
      words.ts                 単語データ
      progress.ts              学習記録の純粋関数
      WordCard.tsx             単語カード UI
      *.test.ts(x)             同じディレクトリに併置
```

テストは対象ファイルと同じディレクトリに `*.test.ts` / `*.test.tsx` として置く。

## コーディング方針

- **ロジックは純粋関数に寄せる。** 状態遷移（正解・不正解の記録、出題順の決定、
  復習間隔の計算など）は React から切り離した純粋関数として書き、単体テストする。
  UI コンポーネントのテストは「その純粋関数が画面に正しくつながっているか」を見る。
- **型は strict のまま。** `any` と `as` による握りつぶしを使わない。
- ユーザーに見える文言は日本語、学習対象の語句はフランス語（`lang="fr"` を付ける）。

---

# ループエンジニアリングの運用ルール

このプロジェクトは、**受け入れ基準 → Implementer → Reviewer → 反復 → 人間の最終判断**
というループで開発する。以下は例外なく守る。

## 1. 実装後は必ず Reviewer 役として辛口に自己判定する

実装が終わったら、実装者の立場を離れて Reviewer 役に切り替え、
**渡された受け入れ基準に照らして** 一項目ずつ判定する。

- **実装者としての自己弁護をしない。** 「意図的にこうした」「これは仕様外」
  「本質的には動いている」といった弁明は判定材料にしない。
  基準を満たしているか、いないか、だけを書く。
- 基準に書かれていないが壊れているものを見つけたら、それも報告する。
  「基準に書いていないから合格」とはしない。
- 迷ったら不合格にする。**疑わしきは不合格。**

## 2. 判定の根拠は口頭の「できました」ではなく、実行したテストの結果

- 「実装しました」「動くはずです」「通ると思います」は判定の根拠にならない。
- 合格・不合格の根拠として、**実際に実行したコマンドとその出力**を示す。
  最低でも `npm run verify`（または `npm test` と `npm run lint`）を実行し、
  終了コードと結果を引用する。
- テストを実行せずに合格判定を出してはならない。
- 受け入れ基準に対応するテストが存在しない場合、その基準は「検証不能」であり、
  **合格ではない**。テストを追加してから判定し直す。
- テストを通すためにテストを弱めたり、スキップしたり、削除したりしない。
  それが必要に見えるときは、修正ではなく報告すべき状況である。

## 3. 1タスクにつき修正は最大5回まで

- Implementer → Reviewer の往復は、**1タスクにつき最大5回**とする。
- 何回目の反復かを毎回明示する（例: `反復 3/5`）。
- 5回で合格しなければ、そこで**止める**。勝手に6回目に進まない。
  止めるときは以下を報告して指示を仰ぐ:
  1. 受け入れ基準のうち、どれが満たされていないか
  2. 5回で何を試し、それぞれなぜ駄目だったか
  3. 行き詰まりの原因についての見立て
  4. 考えられる選択肢（基準の見直し / 設計変更 / 技術選定の変更 など）

## 4. Reviewer の合格判定は提案であって、完了承認ではない

- Reviewer が「合格」と判定しても、それは**人間への提案**にすぎない。
- **最終判断は必ず人間が行う。** 人間の承認なしに、タスクを完了扱いにしない。
  次のタスクに勝手に進まない。マージや公開の操作をしない。
- 報告には、人間が判断するために必要な材料を必ず添える:
  実行したコマンドと出力、基準ごとの判定、判断に迷った点、残っている懸念。

## タスクの進め方（標準の型）

1. **人間が受け入れ基準を提示する。** 基準は検証可能な形で書く。
2. **Implementer** が実装し、受け入れ基準に対応するテストを書く。
3. **Implementer** が `npm run verify` を実行する。
4. **Reviewer** が基準ごとに判定する。根拠は 3 の出力。
5. 不合格なら 2 に戻る（最大5回、回数を明示）。
6. 合格したら、判定結果と根拠を添えて**人間に提出し、判断を待つ**。

サブエージェントの定義は `.claude/agents/implementer.md` と
`.claude/agents/reviewer.md` にある。
